import React, { Component } from 'react';
import './Etc.scss';

export default class Etc extends Component {
  render() {
    return (
      <div className="Etc">
        etc.
      </div>
    );
  }
}