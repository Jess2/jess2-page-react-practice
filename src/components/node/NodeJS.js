import React, { Component } from 'react';
import './NodeJS.scss';

export default class NodeJS extends Component {
  render() {
    return (
      <div className="NodeJS">
        NodeJS
      </div>
    );
  }
}